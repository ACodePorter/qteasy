# coding=utf-8
"""references 历史数据章节翻译条目（msgid 精确匹配）。"""

HISTORICAL_DATA_TYPES = {'数据类型是qteasy的一个核心概念，它代表一个交易策略中所使用的信息，例如历史交易数据、宏观经济数据等。 信息并不等同于数据，在过去的版本中，`qteasy`将数据类型直接定义为DataSource数据表中的数据列， 但这样的定义方式并不妥当：因为信息与数据是不同的。原因包括：': 'Data types are a core qteasy concept: information used in a trading strategy, such as historical market data or macroeconomic series. Information is not the same as raw data. In older versions, `qteasy` equated data types with DataSource table columns, which was inadequate because:', '某些数据本身并没有足够的意义，需要与其他数据结合才能产生足够的信息': 'Some raw fields alone carry little meaning and must be combined with other data', '有些数据包含多重信息，可以从不同的角度取提取，因此尽管很多情况下数据本身就包含了足够的信息': 'Some fields encode multiple kinds of information extractable from different angles', '信息的定义也是动态的，同样的数据经过不同的方式可以有不同的解读': 'The meaning of information is dynamic—the same data can be interpreted differently', '因此，将信息与数据等同起来是草率的，因为从逻辑上来说，数据仅仅是信息的载体而不是信息本身。': 'Equating information with data is careless: logically, data is only a carrier of information.', '在qteasy新的定义中，数据类型就是我们希望从庞杂的数据中提取的各种信息，有些信息可以直接从数据表中读取，例如：': 'In the new qteasy definition, a data type is information extracted from complex stored data. Some can be read directly from tables, for example:', '股票的开盘价，可以直接从股票价格表中读取': 'Stock open price, read directly from the price table', '但是有些数据必须通过不同表中不同的数据通过组合来读取，例如：': 'Some data must be assembled from multiple tables, for example:', '股票的复权价格，必须读取股票的价格，然后读取股票价格复权因子，并通过计算得到': 'Adjusted stock price requires price data, adjustment factors, and computation', '有些时候从同一列数据中可以读取出不同的信息，例如：': 'Sometimes one column yields different information, for example:', '昨收价格和每日涨幅是不同的信息，但都可以从同一个股票价格表中提取': 'Previous close and daily change are different information from the same price table', '因此，`qteasy`将数据类型重新进行了定义，并将其定义为我们需要的信息，提供简单易用的API，方便用户从数据表中 快速提取所需信息，同时，允许用户创建自定义数据类型，以方便地提取自己感兴趣的信息。 另外，数据类型提供的API还将允许用户很容易地将提取的信息生成为图表、进行对比、比较以及统计分析': 'qteasy redefines data types as the information we need, with simple APIs to extract it quickly, support custom types, and chart, compare, and analyze results.', '数据类型的定义应该通过一个类来实现，因而用户自定义可以较容易实现。同时，qteasy应该提供相当完善的内置数据类型以减少用户自定义工作量，且覆盖尽可能多的数据表确保下载的数据能够物尽其用。': 'Each data type is implemented as a class for easy customization. qteasy also ships extensive built-in types covering many tables so downloaded data is fully usable.', '从逻辑上讲，一个数据类型的定义需要包含以下关键要素：': 'Logically, defining a data type requires:', '数据来源：该信息来自于那些数据表中的哪些数据字段？': 'Data source: which tables and columns provide this information?', '数据获取方式：如何提取该信息？': 'Acquisition: how is the information extracted?', '鉴于大量的内置数据类型需要被定义，那么这些数据类型的定义必须是简单地，参数化的，所幸数据类型类并不也不会太复杂，应该容易实现参数化定义。': 'Many built-in types must be defined simply and parametrically; the class model stays small enough for parameter-driven definitions.', '用法和输出 首先是数据类型的用法和输出，看起来应统一为两个API：': 'Usage and output — data types expose two unified APIs:', 'API1: `data_type.get(start, end, frequency)`:获取该类型与个股无关的，区间内的单频数据，输出为类似Series 的数据。': 'API1: `data_type.get(start, end, frequency)` — symbol-agnostic interval data at one frequency, Series-like output.', 'API2: `data_type.get(shares, start, end, frequency)`: 获取该类型与给定个股有关的区间内单频数据。': 'API2: `data_type.get(shares, start, end, frequency)` — per-share interval data at one frequency.', '也许还有第三个API： `data_type.get(shares, start, end, frequency, **kwargs)`': 'A third API: `data_type.get(shares, start, end, frequency, **kwargs)`', '以上API应该完全统一，不管是内置还是自定义类型都可返回相同格式的数据。': 'Built-in and custom types share the same return format.', '数据类型的类型 其次应该研究的是数据类型的类型，有多少种不同类型的数据类型？当然，这里的类型取决于从数据表中获取数据的方式和产出，如股价数据和成份数据的获取方式就不同，前者直接用数据标签从表中读取，而后者需从表中筛选后再通过行列变换得到。当然，还有可能更多的方式，如通过多表查询如复权价格或多数据计算得到。 这里只是说，明显不同的数据类型都有各自的特点，一个类就必须分别处理。': 'Acquisition categories depend on how data is read and produced—direct reads vs composition tables, multi-table queries such as adjusted prices, etc. Distinct patterns need distinct handling.', '如果不考虑多数据计算类的数据类型的话（因为多数据计算可以在策略中实现，但双数据计算型的特殊型：复权价格又应含在内），数据类型的类型也许有下面几种：': 'Ignoring pure multi-field math types (except adjusted price), acquisition types include:', '以下属性被称为数据的获取类型（acquisition_type）这些类型的数据可以输出symbolised或者un-symbolised数据，且 这些数据都支持输出时间段数据(Periods)以及时间戳数据(TimeStamp)：': '`acquisition_type` controls symbolised vs un-symbolised output and Period vs TimeStamp layouts:', '在指定数据类型时，这种数据类型可以支持的输出类型是在数据类型定义中就确定了的，无法更改': 'Supported output layouts are fixed at data-type definition time', '基本数据型。这些数据与时间无关，原则上不属于历史数据，仅作为信息调用。 这种类型的数据只需要一个表名和列名作为kwargs读取参数': '`basics` — time-independent reference data; kwargs: one table and column', '直读数据型。也就是根据区间频段直接从数据表内直接读取的数据，如价格或指标 这种类型的数据类型只需要从一个表名和列名中读取数据即可，因此读取参数只有一个表名和列名': '`direct` — read one table/column over an interval and frequency', '数据修正型。从一张表中直读数据A，另一张表直读数据B，并用B修正后输出，如复权价格 这种类型的数据需要从两张表中读取数据，因此读取参数有两个表名和列名': '`adjustment` — read A and B from two tables and adjust A with B (e.g. adjusted price)', '数据关联型。从两张表中读取数据A与B，并输出它们之间的某种关系，如eq/ne/gt/or/nor等等': '`relations` — relational output between A and B (eq/ne/gt/or/nor, etc.)', '数据计算型。从两张表中读取数据A与B，并输出他们之间的计算结果，如+/-/*//': '`operation` — arithmetic between A and B (+/-/*//)', '事件状态型。从表中查询事件的发生日期并在事件影响区间内填充不同状态的，如停牌，改名等': '`event_status` — fill status over event impact windows (suspension, rename, etc.)', '多事件状态型。从表中查询多个事件的发生日期并在事件影响区间内填充多个不同的状态，如管理层名单等': '`event_multi_stat` — multiple statuses over event windows (e.g. management roster)', '事件信号型。从表中查询事件的发生日期并在事件发生时产生信号的，如涨跌停，上板上榜，分红配股等': '`event_signal` — signals at event dates (limit up/down, listings, dividends, etc.)', '成份查询型。从成份表中筛选出来数据并行列转换 另外还应该有一些特殊类型，由特殊的过程实现': '`composition` — filter composition tables and pivot rows/columns; plus special procedural types', '单时刻复合类型。查找一个时间点上可用的多种数据并组合输出，如个股某时刻的财务报表': '`compound` — combine multiple fields at one timestamp (e.g. financial statements)', '以时间段为单位的symbolised数据，这种数据结构类似一个DataFrame，行为时间段，列为股票代码': 'Symbolised Period data: DataFrame-like, rows = time periods, columns = share codes', '以时间段为单位的un-symbolised数据，这种数据结构类似于一个Series，行为时间段，只包含单列数据': 'Un-symbolised Period data: Series-like, rows = time periods, single column', '数据类型类的定义 类型的定义为了简单，应该是参数化的，只要参数定了，读取方式和读取目标表就确定了': 'Class design is parametric: parameters fix acquisition method and target tables', '类的唯一一个可被外部调用的方法只有一个，这个方法根据不同的读取方式调用不同的方法，从目标表中读取数据，并清洗后输出。': 'One public method dispatches by acquisition type, reads target tables, cleans, and returns data.', '甚至，不同的数据特点都可以定义在类型中，如open可以在最新的cycle中使用，而close则不可以': 'Type metadata can encode field semantics, e.g. `open` usable in the latest cycle but `close` not', '为了可扩展性，类中也可以定义一个空白函数，用于特殊情况下由用户重写后自定义数据读取方式': 'A hook method may be overridden for custom acquisition logic', 'qteasy内置数据类型有非常多，为了方便这些类的创建，这里创建一个数据类型映射表，将所有的数据类型与其关键参数映射起来， 这样就可以通过一个简单的函数调用来创建一个数据类型类了。': 'Many built-in types are created via a mapping table from type IDs to key parameters.', 'DATA_TYPE_MAP:          定义数据类型与数据表之间的对应关系，以查询每种数据可以再哪一张表里查到                         每种数据类型都有一个唯一的ID，且每种数据类型都只有一个唯一的存储位置': '`DATA_TYPE_MAP` maps each data type ID to its storage table (unique ID and location per type).', 'Data table mapping中各列的含义如下： htype_name(key):            数据类型名称（主键）': 'Data table mapping columns: `htype_name` (key): data type name', 'freq(key):                  数据的可用频率（主键）                             min - 分钟数据                             d - 日数据                             w - 周数据                             m - 月数据                             q - 季度数据': '`freq` (key): available frequency — min/d/w/m/q', 'asset_type(key):            数据对应的金融资产类型:                             E - 股票                             IDX - 指数                             FT - 期货                             FD - 基金                             OPT - 期权                             THS - 同花顺指数                             SW - 申万行业指数': '`asset_type` (key): E, IDX, FT, FD, OPT, THS, SW, etc.', 'description:                历史数据的详细描述，可以用于列搜索': '`description`: detailed description for search', 'acquisition_type:           数据的获取方式': '`acquisition_type`: acquisition method', 'table_name:                 历史数据所在的表的名称': '`table_name`: storage table name', 'column:                     历史数据在表中的列名称': '`column`: column name in the table'}

GET_HISTORY_DATA = {'在 qteasy 2.x 中，`qt.get_history_data()` 的推荐用法是直接返回 `HistoryPanel`：': 'In qteasy 2.x, `qt.get_history_data()` is recommended to return a `HistoryPanel` directly:', '`HistoryPanel` 是一个三维数据容器，维度含义为：': '`HistoryPanel` is a 3-D container with axes:', '**axis 0（shares）**：标的列表，例如股票或指数代码；': '**axis 0 (shares)**: instrument list, e.g. stock or index codes', '**axis 1（hdates）**：时间轴，每一行对应一个时间点（如交易日或分钟）；': '**axis 1 (hdates)**: time axis — one row per timestamp', '**axis 2（htypes）**：历史数据类型，例如 `open`、`high`、`low`、`close`、`vol` 等。': '**axis 2 (htypes)**: history data types such as `open`, `high`, `low`, `close`, `vol`', "使用 `hp[...]` 按轴切片时，结果类型为 **子 `HistoryPanel`**（带正确的轴标签），不再直接得到 `ndarray`。需要裸矩阵时请使用 **`hp['close'].values`** 或 **`hp['close'].to_numpy(copy=True)`**；具名切片与默认拷贝行为见 **`hp.subpanel(..., copy=True)`**。": "Slicing with `hp[...]` returns a **sub-`HistoryPanel`** with correct axis labels, not a raw `ndarray`. Use `hp['close'].values` or `hp['close'].to_numpy(copy=True)` for arrays; see `hp.subpanel(..., copy=True)` for named slices and copy behavior.", '研究向布尔掩码与后续带 **`mask=`** 的 API 可使用 **`hp.where(condition)`**：返回与 **`hp.values`** 同形的 **`dtype=bool`** 数组（不改变 `hp`），详见 [HistoryPanel API 参考](../api/HistoryPanel.rst) 与教程 [使用 HistoryPanel 操作和分析历史数据](../tutorials/2.5-historypanel-data-analysis.md)。': 'For research masks and APIs with `mask=`, use `hp.where(condition)` — a bool array matching `hp.values` without mutating `hp`. See [HistoryPanel API](../api/HistoryPanel.rst) and [HistoryPanel tutorial](../tutorials/2.5-historypanel-data-analysis.md).', "自 **2.2.8** 起还可：**合法标识符列名**用只读属性（如 **`hp.close`**，等价 **`hp['close']`**）；**比较运算**（如 **`hp.close > 100`**、两列子面板比较）得到 **`numpy` 布尔数组**，可直接作为 **`hp.where(...)`** 的输入；按 **交易日（时间轴）** 筛选可用 **`hp.loc[key]`**（等价 **`hp[:, :, key]`**，与 pandas 仅类比）。边界说明见 API 页「列属性访问、比较与 `loc`」与教程 §6.1。": "Since **2.2.8**: read-only column attributes (`hp.close` ≡ `hp['close']`); comparisons (`hp.close > 100`) yield numpy bool arrays for `hp.where(...)`; time filtering via `hp.loc[key]` (≡ `hp[:, :, key]`). See API “column access, comparisons, and loc” and tutorial §6.1.", '这样，策略与可视化都可以在同一份结构化数据上工作，避免重复转换。': 'Strategies and visualization share one structured object without repeated conversions.', '当你已有 `DataFrame` 或字典形式的数据时，可以使用 `qt.dataframe_to_hp()` 将其适配为 HistoryPanel，再复用同一套 API。': 'Adapt existing `DataFrame` or dict data with `qt.dataframe_to_hp()` and reuse the same API.', '从 HistoryPanel 到可视化': 'From HistoryPanel to Visualization', '`HistoryPanel` 提供统一的可视化入口 `hp.plot()`，根据面板中已有的 htypes 自动选择合适的图表类型（K 线、成交量、MACD 或普通折线），并支持多标的对比：': '`HistoryPanel.plot()` picks chart types (candlestick, volume, MACD, line) from existing htypes and supports multi-instrument comparison:', 'HistoryPanel.plot 交互式可视化（Plotly）': 'HistoryPanel.plot Interactive Visualization (Plotly)', "当你使用 `hp.plot(interactive=True)` 时，qteasy 会使用 Plotly 生成交互式图表，支持缩放、平移与悬停查看数据。在 Notebook 中，如果你的环境支持 FigureWidget（通常需要安装 `ipywidgets` 与 `anywidget`），qteasy 会优先返回更完整的交互体验；否则会回退到 HTML 方式展示。你也可以通过 `plotly_backend_app='auto'|'FigureWidget'|'html'` 明确指定输出方式。": "`hp.plot(interactive=True)` uses Plotly with zoom, pan, and hover. In notebooks, FigureWidget (needs `ipywidgets` and `anywidget`) is preferred; otherwise HTML fallback. Set `plotly_backend_app='auto'|'FigureWidget'|'html' explicitly if needed.", '依赖与安装': 'Dependencies and Installation', '**基础交互（Plotly Figure）**：': '**Basic interactivity (Plotly Figure)**:', '**Notebook 更完整交互（FigureWidget + 回调）**：': '**Full notebook interactivity (FigureWidget + callbacks)**:', '注意：若未安装 Plotly，调用 `interactive=True` 会直接抛出英文错误提示（例如包含 “requires plotly” 的信息）；这是为了让依赖缺失问题尽早暴露，避免生成一个不可交互的“空图”。': 'If Plotly is missing, `interactive=True` raises an English error (e.g. “requires plotly”) so missing dependencies fail early instead of producing a non-interactive empty chart.', '`plotly_backend_app`：输出方式与回退': '`plotly_backend_app`: output mode and fallback', '`plotly_backend_app` 仅在 `interactive=True` 时生效：': '`plotly_backend_app` applies only when `interactive=True`:', "`plotly_backend_app='auto'`：在 Notebook 中优先使用 FigureWidget；否则回退到 HTML 包装；在脚本环境中也可能返回原始 Plotly Figure。": "`plotly_backend_app='auto'`: prefer FigureWidget in notebooks; else HTML wrapper; may return raw Plotly Figure in scripts.", "`plotly_backend_app='FigureWidget'`：强制使用 FigureWidget；若当前不是 Notebook 或缺少依赖会直接报错（英文提示）。": "`plotly_backend_app='FigureWidget'`: force FigureWidget; error if not in notebook or deps missing.", "`plotly_backend_app='html'`：强制使用 HTML 包装；若当前不是 Notebook 也会直接报错（英文提示）。": "`plotly_backend_app='html'`: force HTML wrapper; error if not in notebook.", '关键交互能力（用户视角）': 'Key Interactive Features (User View)', '**缩放/平移一致性**：HTML 与 FigureWidget 在缩放/平移时遵循同一套 x 轴约束：保持最少可见 bar 数、越界时按 pan 语义平移回数据域。': '**Zoom/pan consistency**: HTML and FigureWidget share x-axis constraints — minimum visible bars, pan back into data range when out of bounds.', '**顶部 OHLC 摘要**：当面板中存在完整 OHLC 主图（蜡烛图）时，会显示顶部摘要；交互图初始为最后一根 bar，点击某根 bar 会更新摘要。无 K 线主图（例如仅 close 折线）时不显示该摘要区。': '**Top OHLC summary**: shown when full OHLC candlestick main chart exists; starts at last bar, updates on click. Hidden for line-only charts (e.g. close only).', "**两标的 overlay**：当你用 `layout='overlay'` 对比两只标的时，点击某根 bar 会切换主次标的：主次透明度与线宽会联动更新；如果你启用了 `highlight`，高亮点默认仅对当前主标的可见，并随主次切换。": "`layout='overlay'` for two instruments: clicking a bar switches primary/secondary; opacity and line width update; `highlight` follows the primary instrument.", '**选中态十字交叉线**：点击后在主价格图上显示选中态十字交叉线；缩放/平移时保持同步；当选中 bar 滚动出可见区域时会隐藏。': '**Selection crosshair**: shown on the main price chart after click; syncs on zoom/pan; hidden when the selected bar scrolls out of view.', '示例：最常用的两种交互调用': 'Examples: Two Common Interactive Calls', '**示例 1：单标的交互式 K 线**': '**Example 1: single-instrument interactive candlestick**', '**示例 2：两标的 overlay + 高亮**': '**Example 2: two-instrument overlay + highlight**', '可视化层严格遵循“**有什么画什么**”原则：': 'Visualization follows “**plot what exists**”:', '只使用 HistoryPanel 中**已经存在**的列，不在绘图层计算新的指标；': 'Only columns already in the HistoryPanel — no new indicators computed in the plot layer', '是否绘制 K 线、成交量或 MACD，仅取决于面板中是否存在对应的 htypes。': 'Candlestick, volume, or MACD depends solely on corresponding htypes present', '典型 htypes 与默认图表类型的对应关系如下：': 'Typical htype-to-chart mapping:', '图表类型': 'Chart type', '依赖的 htype 示例': 'Example htypes required', 'K 线': 'Candlestick', '成交量': 'Volume', '折线图': 'Line chart', '其它任意一维序列（如 `close`、`pe` 等）': 'Any 1-D series (e.g. `close`, `pe`)', '若需要 MA/布林带/MACD 等指标，应在 HistoryPanel 上调用相应方法 （例如 `hp.kline.ma()`、`hp.kline.bbands()`、`hp.kline.macd()`）， 将结果列追加进面板后再调用 `hp.plot()`。': 'For MA/Bollinger/MACD, compute on HistoryPanel (`hp.kline.ma()`, `hp.kline.bbands()`, `hp.kline.macd()`), append columns, then `hp.plot()`.', '`qt.candle` 与 HistoryPanel 的关系': 'Relationship between `qt.candle` and HistoryPanel', '`qt.candle()` 是更高层的快捷入口，用于“一行代码画出某个标的的 K 线图”， 其内部逻辑可以概括为：': '`qt.candle()` is a high-level shortcut to plot one instrument’s candlestick in one line:', '解析 `stock`、`start`、`end` 等参数，从本地数据源取数；': 'Parse `stock`, `start`, `end`, fetch from local DataSource', '将价格数据适配为单标的 `HistoryPanel`；': 'Adapt prices to a single-instrument HistoryPanel', '在 HistoryPanel 上追加用户需要的指标列（如 MA、布林带、MACD）；': 'Append requested indicators (MA, Bollinger, MACD) on HistoryPanel', '调用 `hp.plot(...)` 绘制图表。': 'Call `hp.plot(...)` to render', '因此，你可以在保留现有 `qt.candle` 调用方式的同时，直接在 HistoryPanel 上 做更细粒度的控制。': 'Keep existing `qt.candle` calls or control plotting directly on HistoryPanel.', '常见用法示例：': 'Common usage examples:', '其中 `plot_type` 的意义为：': '`plot_type` values:', "`'candle'` / `'c'`：使用 OHLC 绘制蜡烛图；": "`'candle'` / `'c'`: OHLC candlestick", "`'ohlc'` / `'o'`：行为与 `'candle'` 接近，仅作为轻量别名保留；": "`'ohlc'` / `'o'`: lightweight alias near `'candle'`", "`'line'` / `'l'`：只绘制一维价格折线（通常为 `close`）；": "`'line'` / `'l'`: 1-D price line (usually `close`)", "`'none'` / `'n'`：**只取数不绘图**，直接返回 `DataFrame`，方便你用 其它工具自行可视化。": "`'none'` / `'n'`: **data only**, return `DataFrame` for external plotting", "Renko 图（`'renko'` / `'r'`）在新版中已不再内置支持；若需要 Renko 图， 建议使用专门的技术分析或画图库。": "Renko (`'renko'` / `'r'`) is no longer built in; use a dedicated TA/charting library.", '`qt.refill_data_source()`的`tables`参数指定需要补充的数据表； 除了直接给出表名称以外，还可以通过表类型同时下载多个数据表的数据：': '`qt.refill_data_source()` `tables` selects tables to refill — by name or by group:', '`cal`     : 交易日历表，各个交易所的交易日历': '`cal`: trading calendars per exchange', '`basics`  : 所有的基础信息表，包括股票、基金、指数、期货、期权的基本信息表': '`basics`: basic info tables (stocks, funds, indices, futures, options)', '`adj`     : 所有的复权因子表，包括股票、基金的复权因子，用于生成复权价格': '`adj`: adjustment factor tables for adjusted prices', '`data`    : 所有的历史数据表，包括股票、基金、指数、期货、期权的日K线数据以及周、月K线数据': '`data`: historical OHLCV tables (daily/weekly/monthly)', '`events`  : 所有的历史事件表，包括股票更名、更换基金经理、基金份额变动等数据': '`events`: corporate/event tables (renames, manager changes, fund shares, etc.)', '`report`  : 财务报表，包括财务指标、三大财务报表、财务快报、月报、季报以及年报': '`report`: financial statements and reports', '`comp`    : 指数成分表，包括各个指数的成份股及其百分比': '`comp`: index composition and weights', '`all`     : 所有的数据表，以上所有数据表，由于数据量大，建议分批下载': '`all`: all tables — large download; refill in batches', '数据下载到本地后，可以使用`qt.get_history_data()`来获取数据，如果同时获取多个股票的历史数据，每个股票的历史数据会被分别保存到一个`dict`中。': 'After download, use `qt.get_history_data()`; multiple shares return a `dict` keyed by code.', '例如，通过检查发现，`stock_daily`表中的数据不够完整，最新的数据仅更新到2022年3月22日，数据共有1210万行：': 'Example: `stock_daily` ends at 2022-03-22 with ~12.1M rows', '此时使用`qt.refill_data_source()`函数，批量获取2022年3月以后一直到10月底的`stock_daily`数据': 'Use `qt.refill_data_source()` to refill `stock_daily` from Mar 2022 through Oct 2022', '数据下载过程中会通过进度条显示下载进度，同时显示下载读取的数据量和总耗时。由于数据需要分批下载，下载大量的数据可能非常耗时。默认情况下 qteasy会通过多线程并行下载提高速度，但下载频率过快可能会导致tushare权限问题。': 'Refill shows progress, row counts, and elapsed time. Large jobs are slow; parallel threads speed downloads but may hit Tushare rate limits.', '下载完成后重新检查`stock_daily`的数据：可以发现数据已经增加到1280万行，增加了70万行数据，数据覆盖的日期也达到了2022年10月31日。': 'After refill, `stock_daily` grows to ~12.8M rows (+700k) through 2022-10-31.', '本地数据的获取和可视化': 'Local Data Access and Visualization', '只要本地数据源中有了数据，就可以非常容易地获取这些数据，如果是量价数据，还可以非常容易地可视化（打印为K线图或价格曲线图）': 'Once data is local, fetch it easily; OHLCV can be visualized as candlesticks or lines', '股票或资产的基本信息数据': 'Basic security information', '获取一个股票或证券的基本信息，`code_or_name`可以是股票的代码，或者股票名称，默认情况下系统会进行全剧匹配查找匹配的证券，如`‘000001’`会匹配到多种不同的证券，所有匹配的证券都会被列出，除非在`asset_type`中指定一种或几种证券类型。 如果设置`match_full_name=True`，则会通过股票或基金的全名来模糊匹配证券': '`get_basic_info(code_or_name)` accepts code or name; global match lists all hits unless `asset_type` narrows scope; `match_full_name=True` fuzzy-matches full names', '`get_basic_info()`的别称': 'Alias of `get_basic_info()`', '筛选股票代码': 'Filter stock codes', '根据发行日期、地区、行业、规模、指数成份等多种可用的信息筛选出一部分股票，打印出股票的信息': 'Filter stocks by listing date, region, industry, size, index membership, etc., and print info', '根据发行日期、地区、行业、规模、指数成份等多种可用的信息筛选出一部分股票，返回股票的完整代码': 'Same filters, return full stock codes', '提取金融历史数据': 'Extract historical financial data', '这个函数从默认`QT_DATA_SOURCE`中提取数据，根据输入的数据类型、股票代码、日期范围以及频率等参数，输出多个`DataFrame`或`HistoryPanel`对象 这些`DataFrame`保存在一个dict中，这个dict的键为股票代码或数据类型，值为按股票代码或数据类型分组的数据框': 'Reads from default `QT_DATA_SOURCE` by data type, codes, date range, and frequency; returns `DataFrame` or `HistoryPanel` objects in a dict keyed by code or type', '量价数据的可视化': 'OHLCV visualization', '这个函数从本地数据源中提取股票的价格数据，并生成一张完整的动态高级K线图': 'Fetch local price data and render a full dynamic advanced candlestick chart', '示例：': 'Example:', '查找证券的基本信息': 'Look up basic security information', '使用`qt.get_basic_info()`获取证券的基本信息 在`DataSource`中准备好相关的金融数据后，还可以方便地查找证券的信息，通过证券代码（六位数字）或证券的名称搜索证券，并打印出详细信息。 使用证券名称搜索时，支持模糊查找或使用通配符：': 'With data in `DataSource`, `qt.get_basic_info()` finds securities by six-digit code or name (fuzzy or wildcard search supported)', '例如，通过六位代码获取证券信息：': 'Example: lookup by six-digit code', '也可以直接通过证券名称来查找证券，模糊查找匹配多个证券，例如：': 'Or search by name — fuzzy match may return multiple hits, e.g.:', '通过证券名称匹配证券时，支持通过通配符查找：': 'Wildcard search by name is supported', '默认情况下，并不会查找证券的全名，如果有必要，可以匹配全名，例如:': 'By default full names are not matched; set full-name matching when needed, e.g.:', '结果如下：': 'Output:', '`qt.filter_stocks`被用来根据某些标准筛选股票，例如：': '`qt.filter_stocks` filters stocks by criteria, e.g.:', '生成的结果是一个`dict`，默认情况下以股票代码为`key`，`value`是一个包含所有需要类型的历史数据的`DataFrame`：': 'Result is a `dict` keyed by stock code; each value is a `DataFrame` of requested types', '如果需要同时读取多个股票的多种数据，也可以实现。而且可以设置不同的数据频率': 'Multiple shares and types at different frequencies are supported', '保存到本地的股票量价数据可以直接以K线图的形式显示出来，下面是一些示例：': 'Local OHLCV can be shown as candlesticks — examples below', '历史价格数据的可视化': 'Historical price visualization', '`qteasy`支持使用动态K线图展示数据，此时不需要从`DataSource`中读取数据，直接使用`qt.candle()`函数即可实现：': 'Dynamic candlesticks via `qt.candle()` without manual DataSource reads', '`qt.candle()`函数支持传入K线图的开始日期、结束日期、K线频率、复权方式以显示不同区间和频率的K线图，也支持传入移动均线的时长和macd的不同参数显示不同的均线，例如，传入下面的参数显示一段时间内的60分钟K线图，且使用后复权价格:': '`qt.candle()` accepts start/end, frequency, adjustment, MA periods, MACD params — e.g. 60-minute post-adjusted chart:', '`qt.candle()`函数还支持通过股票名称显示K线图，如果输入是股票名称，会自动模糊查找，并且支持通配符。例如：': '`qt.candle()` accepts names with fuzzy/wildcard lookup, e.g.:', '下面是更多的K线图例子，展示了股票、基金、指数等不同的资产类别，不同的数据频率，不同的均线设定、不同的图表类型等': 'More candlestick examples: stocks, funds, indices, frequencies, MAs, chart types'}
